// Placeholder — game loop and pet logic implemented in T2 (fuvi-hoz.2)
const canvas = document.getElementById('canvas') as HTMLCanvasElement;
canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

export {};
