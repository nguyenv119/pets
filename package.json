{
  "name": "pets",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "gh-pages": "^6.1.1",
    "typescript": "^5.3.3",
    "vite": "^5.0.0",
    "vitest": "^3.2.4"
  }
}
