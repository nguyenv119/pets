{
  "name": "pixel-pets-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit && node build.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^25.6.2",
    "esbuild": "^0.25.0",
    "jsdom": "^27.0.1",
    "typescript": "^5.3.3",
    "vite": "^5.0.0",
    "vitest": "^3.2.4"
  }
}
